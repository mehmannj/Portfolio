// Gemini configuration for client-side checks only.
// NOTE: Do NOT initialize the Gemini SDK in the browser. Use a server-side proxy (Netlify Function) for secure calls.
// Client-side placeholder — do NOT use this value to initialize the Gemini SDK
// in browser code. All Gemini SDK calls must be performed server-side to keep
// API keys secret. The server function uses `process.env.GEMINI_API_KEY`.
export const GEMINI_API_KEY = ''
export const GEMINI_MODEL = 'gemini-2.5-flash'

