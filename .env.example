# Example environment variables for local development
# Copy this file to .env (or .env.local) and set your actual keys.

# Gemini API key (server-side only). Do NOT expose this to the browser.
# Set this in your hosting provider (Netlify/GitHub Actions) as `GEMINI_API_KEY`.
GEMINI_API_KEY=

# If you are intentionally using Gemini from the client (NOT recommended),
# you can set a Vite-prefixed variable for local/hard-to-avoid cases. Be aware
# that any `VITE_` variable is exposed to the browser when bundled.
VITE_GEMINI_API_KEY=

# EmailJS (if using EmailJS contact form)
VITE_EMAILJS_SERVICE=
VITE_EMAILJS_TEMPLATE=
VITE_EMAILJS_PUBLIC_KEY=
